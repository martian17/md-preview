[package]
name = "md-preview"
version = "0.1.0"
edition = "2024"

[dependencies]
pulldown-cmark = "0.13.3"  # Fast and GFM compatible
tiny_http = "0.12.0"      # Simple, low-overhead server
webbrowser = "1.2.1"      # Opens the default browser cross-platform
